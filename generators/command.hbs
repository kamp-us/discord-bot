import { SlashCommandBuilder } from "@discordjs/builders";

const {{command-name}} = {
  data: new SlashCommandBuilder()
    .setName("{{command-name}}")
    .setDescription("{{command-description}}"),
  async execute(interaction: any) {
    // add your code
  },
};

export default {{command-name}};